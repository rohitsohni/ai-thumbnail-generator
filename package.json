{
  "name": "ai-thumbnail-generator",
  "private": true,
  "version": "1.0.0",
  "description": "MERN AI thumbnail generator with React, Express, MongoDB support, local auth, saved generations, and free Pollinations AI image generation.",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "lint": "npm run lint --workspace client"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
